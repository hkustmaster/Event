<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hkust.android.event">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialSheetFab">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SignInActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:label="@string/title_activity_sign_up"></activity>

        <activity android:name=".PendingEventDetailActivity"></activity>

        <activity
            android:name=".NewEventActivity"
            android:label="@string/title_activity_new_event"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hkust.android.event.MainActivity" />
        </activity>

        <activity android:name=".DateVotingActivity"></activity>

        <activity android:name=".DetailMessageListActivity"></activity>

        <activity android:name=".DetailParticipantListActivity"></activity>

        <activity android:name=".MyEventDetailActivity"></activity>

        <activity android:name=".CloseDiscussionActivity"></activity>

        <activity android:name=".ExploreEventDetailActivity"></activity>

        <activity android:name=".ChangeProfileActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hkust.android.event.MainActivity" />
        </activity>

        <activity android:name=".ChangePasswordActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hkust.android.event.MainActivity" />
        </activity>
    </application>

</manifest>
