<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hkust.android.event" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialSheetFab" >
        <activity android:name=".MainActivity" >
        </activity>
        <activity
            android:name=".SignInActivity"
            android:label="@string/title_activity_login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:label="@string/title_activity_sign_up" />
        <activity android:name=".EventDetailActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hkust.android.event.MainActivity" />
        </activity>
        <activity
            android:name=".NewEventActivity"
            android:label="@string/title_activity_new_event"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.hkust.android.event.MainActivity" />
        </activity>
        <activity android:name=".DateVotingActivity" />
        <activity android:name=".DetailMessageListActivity" />
        <activity android:name=".DetailParticipantListActivity" />
        <activity android:name=".MyEventDetailActivity" />
        <activity android:name=".CloseDiscussionActivity" />
        <activity android:name=".ExploreEventDetailActivity" >
        </activity>
    </application>

</manifest>
